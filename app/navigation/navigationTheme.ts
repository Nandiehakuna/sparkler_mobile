import { DefaultTheme, Theme } from "@react-navigation/native";

const theme: Theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    // primary: "#e6207c",
  },
};

export default theme;
